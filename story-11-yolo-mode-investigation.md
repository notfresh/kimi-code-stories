---
story_id: 11
title: 我研究了一下 AI Agent 的 YOLO 模式，发现事情没那么简单
date: 2026-09-23
author: notfresh
status: recorded
upstream_repo: MoonshotAI/kimi-code
tags: [kimi-code, yolo, agent, ask-when-needed]
---

## 背景

这是一篇聊天口述成文，记录我研究 YOLO 模式时脑子里的几个来回。

主要是我最近研究那个 yolo 模式它的原理，然后记得记录的啊。

## 正文

我研究了一下 AI Agent 的 YOLO 模式，发现事情没那么简单

最近我在折腾 AI Agent，注意到一个叫 YOLO 模式的选项。一开始我以为它就是个"全自动免打扰"开关，后来发现里面的门道比我想的多。

一开始我理解的 YOLO：关掉所有确认弹窗

我最早的理解很朴素：平时 Agent 想删个文件、跑个命令、调个接口，都会先弹窗问我"行不行？"。YOLO 模式一开，它就不再问了，直接按自己的想法执行到底。名字来源也简单，You Only Look Once，引申过来就是"别废话，放手干"。

这个理解不算错，但太粗糙了。因为我紧接着冒出一个问题：那它做不做安全风险评审？

它不做安全评审，只发一张"甩锅声明"

查下来结论很直接：YOLO 模式的核心设计就是不做安全风险评审。

开启时系统可能会弹一个一次性的风险确认提示，逻辑是"你既然要用，出了事你自己担着"。点过之后就不再出现。这不算评审，顶多算甩锅声明。

部分 Agent 内部确实有个轻量分类器，试图对 rm -rf 这类毁灭性操作踩一脚刹车。但它的定位是补充，不是保障。官方文档依然把 YOLO 定义为"完全没有审查"。准确率未知，可能漏掉变体命令，也可能被绕过。不能当可靠的安全网。

那这不就是裸奔吗？

对，某种意义上就是。全凭大模型决定执行什么，系统直接照做，中间没有人工确认这道墙。

但"裸奔"裸的是人工审查这一层，底层沙箱和权限边界还在。如果 Agent 跑在容器或虚拟机里，它删不了宿主机；如果只给了某个目录的读写权限，它出不了这个圈；如果没给网络权限，它发不出去数据。

所以真正的风险公式是：YOLO + 高权限 + 无沙箱 = 真裸奔。反过来，YOLO + 严格沙箱，风险就可控得多。很多人敢用，是因为任务本身需要连续执行几十步，每步都弹窗确认，人会被烦死，效率也归零。YOLO 本质是用安全换流畅，赌的是模型够聪明、任务够明确、环境够隔离。

但我又看到一个选项：Ask When Needed

这就让我困惑了。我一开始以为它是替代 YOLO 的另一种模式，后来才搞清楚：它是在 YOLO 模式开启后出现的一个选项，不是独立模式。

它的作用是决定 YOLO 到底 YOLO 到什么程度。常规操作自动放行，但碰到特定高危动作，还是会停下来问。所以它更像 YOLO 内部的一个档位或子开关，在裸奔和全程确认之间找一个平衡点。

我目前还没完全确认的

具体是哪个工具、那个选项在界面上的确切位置和文案，我还没最终对上。如果能看到截图或确认工具名，应该就能判断它到底是"模式名"还是"子开关"。

总结

YOLO 模式，就是关掉人工确认，让 Agent 放手干。Ask When Needed，是 YOLO 内部那个"必要时还问一句"的开关，让它在裸奔和全确认之间找平衡。

但是我现在还没有看代码，这只是初步调研的。呃，后续进一步确认，还是看源码核实。
