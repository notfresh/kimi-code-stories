---
story_id: 7
title: How do I build an agent？——我为什么做、又怎么做自己的 coding agent
date: 2026-08-24
author: notfresh
status: recorded
tags: [coding-agent, minimal-agent, memory, context-engineering]
---

# How do I build an agent？——我为什么做、又怎么做自己的 coding agent

> 2026-08-24，一个业余项目的自述

## 序

事情的起因特别朴素：我想看懂 Hermes。

Hermes 是我用得最多的 agent——能接手机、能跑定时任务、能陪我读代码，像个住在电脑里的同事。用着用着就想拆开看看：它到底是怎么干活的？于是我去读它的源码，读着读着发现，它太强大了——强大到读不懂。它的代码里塞满了防御性机制：超时、重试、错误恢复、各种应付现实世界的边角逻辑。这些机制让它很能打，但也让"理解原理"这件事变得特别费劲。

读不懂怎么办？我的选择是：**自己写一个**。把 Hermes 蒸馏一遍——剥掉那些防御性的枝枝蔓蔓，只留下核心骨架，看看一个 agent 到底是怎么转起来的。

## 第一章 先做两个选择

动手之前，先得选参照物。当时其实纠结过 Kimi Code——但它的语言是 NodeJS。我虽然用 AI 辅助写过全栈，但那基本是"照猫画虎"；真要理解原理，语言不熟就是最大的拦路虎，很多功能必须得懂语言本身才看得透。从零开始学一门语言，学习成本太高了。

而 Hermes 是我使用频率最高的工具，天天揣在手机里。学它等于把我每天用的东西彻底看透，性价比拉满。

语言也选最熟的：Python。语法简单，读起来不费脑子——读源码时省下来的脑力，全都能花在理解 agent 的逻辑上。而且我过去吃过一个亏：为了实现一个目的，花太多时间在工具上面。这个教训刻得很深：**要保守一点，把时间花在服务目的本身**。所以第一版就用 Python，把"语言"这个维度上的成本整个清零。

## 第二章 第一版：一个会自己转圈的骨架

第一版做得极其克制：一个最小原型，一个骨架，一个 Loop。

没有循环对话——跑一轮就结束。但它的内部可以自己跑很多轮：接到任务 → 判断要不要调工具 → 调 → 看结果 → 再判断 → 直到认为不需要调工具为止，输出结论。

这一刀切得很准：**把"自己干活"和"跟人说话"分成两层**。第一版只管内层——那个 agent 的"呼吸循环"。它其实已经是个 agent 了，只是还聋哑：不会问你问题，也不会被你打断。但核心机制已经在了：循环 + 判断工具用不用 + 把结果喂回上下文。

对它的测试也很轻量。我没急着让它干复杂活，就是看它能不能用——能跑、能调工具、能转圈、能停，骨架健康就达标了。它的定位是"学习观察对象"：标本，不是牛马。它的价值不在帮我干多少活，而在它的一举一动能让我看懂 agent 的原理。

## 第三章 第一次验证："原来就这些？"

后来我发现它能创建简单的项目、能创建文件——跟商业 agent 的入门级任务，看不出什么区别。

这是个重要的验证时刻。我亲手搭的骨架 + 一个 LLM + 几个基础工具，就能干入门级的活。这说明 agent 的门槛不在魔法，就在那三件套：循环、工具、上下文。商业 agent 那些防御性机制，管的是现实世界的脏活；而"入门任务能干"，靠的就是这套骨架本身。

**蒸馏被验证了**：核心机制全在我手里，丢掉的是锦上添花，不是地基。

## 第四章 V2：从标本到正经的 coding agent

第二步，我开了 V2：把代码拆开，新建仓库，加了两件大事。

一是**会话体系**——会话持久化 + 会话管理。先有"会话"这个东西能存能取，才有"管理多个会话"可言，它俩是一件事的两面。多轮对话接上了，它开始有"过去"了，重启之后还记得聊到哪；多个会话并存、互不干扰，每个会话是独立的上下文世界。

二是**计划模式**——不能随便去改其他代码。这是一个大模块：先读、先想、先输出方案，把"动手改"变成一个需要授权才能做的动作。

现在回头看，这第二个才是重点——**我已经开始给自己写防御机制了**。一开始蒸馏 Hermes 时，我把防御机制当噪音剥掉；现在 V2 开始碰真实代码了，我主动补上了"不能乱改代码"的护栏。这说明两件事：它已经进入真实使用场景了；我也亲身体会到了 Hermes 那些防御机制是怎么长出来的——不是设计者闲的，是真实需求逼出来的。

V2 做完，它越来越像一个正经的 coding agent 了。

## 第五章 V3：接上 superpowers，准备上工

接下来我打算接 superpowers——一个我经常用的 harness 项目，我觉得它很厉害。其实 V2 里就尝试过，但 V2 同时做的事太多了，插件这块做得乱七八糟。我决定开 V3 专门做这件事，认真做到接近"发行"的状态，然后让它真的去干现实中的任务。

我平时会好几个 agent 混着用：Kimi Code、Hermes、Claude Code……综合着来。等 V3 接完 superpowers，我自己的这个也要正式上工了——从解剖台上下岗，去干活。

## 第六章 为什么要做它

说到底，这个项目最初的目的其实是做一个**教学系统**——用来学 agent 原理。但我更想探索的是：**自己手搓一个 coding agent，它的上限到底在哪**。

这也是个业余项目，会给我加分。我特别想通过这件事证明一下自己，看看自己的产品能力。

至于为什么偏偏是 coding agent？因为这是我目前接触到最多的事情，我没有别的东西可以实践。**我做的事，一定是为了解决我现实中的问题**——这是最根本的。而且 coding agent 是最容易闭环验证的场景：agent 会写代码，你让它干活的同时，就是在测试它自己。需求真实、反馈直接、结果看得见。

所以设计的时候我也留了一手：让它方便拓展。核心循环和会话体系是通用的，coding 只是第一层皮肤。哪天我需要别的场景，换工具集、换提示词，骨架不用动。coding agent 是第一个原始应用场景，但未必是最后一个。

## 第七章 敢于亮剑

最近我冒出一个念头：如果 Hermes、Kimi Code 它们本质上也这么简单——没有嵌套多少东西，只是因为品牌才显得厉害——那我能不能做一个任务，跟它们比试比试？

如果大家都是 agent，本身就这么简单，那谁都可以做 agent，包括我。甚至可以想得更远：**用 AI 这个杠杆，撬动一个能力，做出比大公司、比一个团队做的还出彩的东西**。

接下来我的计划就是：**评测 agent**。用自己写的这个还比较粗糙的框架，去和一个团队做的东西比试——在特定的任务上，看能不能领先。看差别在哪里，甚至看看有没有可能通过微调实现超越。

先别猜，跑起来再说。单科作战，轻量版未必输——商业 agent 是全能选手，背着厚厚的防御机制；我的框架是蒸馏过的，在特定任务上可能更直接、更快出活。**全科肯定打不过，单科有机会赢。**

这一步做完，这个作品就不仅仅限于教学作用了——它会有评测数据，有对比结果。有证据的东西，才让人印象深刻。

## 第八章 追梦：记忆管理

其实还有一个更早、更深的动机，我管它叫"追梦"。

很早以前——早到我刚成为一个程序员、成为计算机专业的人的时候——我就想做一件事：**搜索**。因为曾经的一些工作经历，这个念头一直没放下，我特别想去做搜索。

搜索这个东西，放到今天 agent 的语境里，就是**记忆管理**——让 agent 借鉴过往的智慧。往大了说，这就是**上下文工程**：把搜索、记忆、上下文这些事，切合到 agent 的环境里。

所以前面写的那些，可能都只是基础。未来我会更深入地去琢磨记忆管理这一块——不管是在这个项目上，还是去给其他 coding agent、通用 agent 添砖加瓦的时候。

我觉得**上下文就是未来的方向之一**，也是我圆梦的一个归属。

## 终章

回头看这条路：读不懂 Hermes → 自己写一个来懂 → 验证"原来就这么简单" → 长出会话和护栏 → 准备上工 → 打算亮剑评测 → 落回到最初的梦。

每一步都不是计划出来的，是被问题推着走的。而这条路的尽头，我想看看：一个普通人，用 AI 当杠杆，能把一个 agent 做到什么程度——顺便，把年轻时想做搜索的那个梦，圆在 agent 的记忆管理上。
